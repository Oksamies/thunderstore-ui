// Placeholder to match Figma design
// We'll calculate real conflicts/updates later when services are ready.
export const getModStatus = (mods: ManifestV2[]) => {
  // Mock data for now, as requested to implement visual design first
  return {
    conflictCount: 4,
    updateCount: 11,
  };
};
